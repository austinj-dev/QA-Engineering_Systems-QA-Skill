# Concurrency Testing — QA Engineering

- [ ] Open same record in two tabs, edit both, save both — no data loss
- [ ] Double-click submit → only one record created
- [ ] Rapid delete clicks → no double-delete errors
- [ ] Submit button disables during request
- [ ] Two users create same unique record → one gets conflict error
- [ ] Rapid navigation doesn't cause race conditions
- [ ] Password change on Device A → Device B session handled